<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head',{title: 'Crear'}) %> 

<body>
    <%- include('../partials/header') %>
    <h1 class="titulo">Crear Productos</h1>
    <main class="crear">
        <div class="parrafo-form">
            <p class="saludo">¡Bienvenidx ADMIN!</p>           
        </div>
        <form action="/administrador/crear" method="post" class="create-form" enctype="multipart/form-data">
            <div class="control-formulario">
                <label for="floatingSelect">Marca</label>
                <select class="control" name="Marca" id="floatingSelect" aria-label="Floating label select example">
                    <option selected hidden>Selecciona una opcion</option>
                    <option value="Elements">Element</option>
                    <option value="Lab">Lab</option>
                    <option value="Kalima">Kalima</option>
                    <option value="Woodo">Woodo</option>
                    <option value="Ace shcok">Ace shcok</option>
                    <option value="RINGS ACE WAX">RINGS ACE WAX</option>
                    <option value="BONES KOWALSKI">BONES KOWALSKI</option>
                    <option value="REDWINGS">REDWINGS</option>
                    <option value="DATER">DATER</option>
                
                  </select>
            </div>
            <div class="control-formulario">
                <label>Título</label>
                <input type="text" name="titulo" placeholder="Ingrese el título"
                    autocomplete="current-password" value="<%= locals.old && old.titulo ? old.titulo : null%>" id="titulo" class="control" />
                    <p>
                        <%= locals.errors && errors.titulo ? errors.titulo.msg : null %> 
                    </p>
            </div>
            <div class="control-formulario">
                <label for="floatingSelect">Categoria</label>
                <select class="control" name="categoria" id="categoria" >
                    <% if (locals.errors && old.categoria) { %>
                        <option hidden value="">Selecciona una opcion</option>
                        <option <%= old.categoria === "Skates" ? "selected" : null %>  value="Skates">Skates</option>
                        <option <%= old.categoria === "Longboards" ? "selected" : null %>   value="Longboards">Longboards</option>
                        <option <%= old.categoria === "Accesorios" ? "selected" : null %>   value="Accesorios">Accesorios</option>
                        <option <%= old.categoria === "Partes" ? "selected" : null %>   value="Partes">Partes</option>
                    <% }else{ %>
                        <option selected hidden value="">Selecciona una opcion</option>
                    <option value="Skates">Skates</option>
                    <option value="Longboards">Longboards</option>
                    <option value="Accesorios">Accesorios</option>
                    <option value="Partes">Partes</option>
                    <% } %> 
                    <option selected hidden>Selecciona una opcion</option>

                  </select>
                  <p >
                    <%= locals.errors && errors.categoria ? errors.categoria.msg : null %>
                </p>
                
            </div>
            
            <div class="control-formulario">
                <label>Precio</label>
                <input type="number" name="Precio" placeholder="Ingrese el precio"
                value="<%= locals.old && old.Precio ? old.Precio : null%>" id="Precio" class="control" />
                     <p >
                        <%= locals.errors && errors.Precio ? errors.Precio.msg : null %>
                    </p>
            </div>
            <div class="control-formulario">
                <label>Descuento</label>
                <input type="number" name="Descuento" placeholder="Ingrese el procentaje de descuento"
                value="<%= locals.old && old.Descuento ? old.Descuento : 0%>" id="Descuento" class="control" />
                     <p >
                        <%= locals.errors && errors.Descuento ? errors.Descuento.msg : null %>
                    </p>
            </div>
            <div class="control-formulario">
                <label>Stock</label>
                <input type="number" name="Stock" placeholder="Ingrese el stock"
                value="<%= locals.old && old.Stock ? old.Stock : null%>" id="Stock" class="control" />
                     <p >
                        <%= locals.errors && errors.Stock ? errors.Stock.msg : null %>
                    </p>
            </div>
            <div class="control-formulario">
                <label>Descripción</label>
                <textarea class="texto" id="descripcion" name="descripcion" - placeholder="Introduzca una breve descripción del producto" ><%= locals.old && old.descripcion ? old.descripcion : null%></textarea >
                    <p >
                        <%= locals.errors && errors.descripcion ? errors.descripcion.msg : null %>
                    </p>
                </div>
                
            </div>
            <div class="col col-lg-7 col-md-12 col-sm-12">
                        
                <section class="img-product">

                    <div class="container">
                        <div class="row">

                            <!-- Imagen 1 -->

                            <div class="col col-5">
                                <label class="selectImg" for="product-img">
                                    <img id="img-preview" class="img-fluid img-thumbnail imagen-height rounded" src="/images/productos/default-image.png" alt="">
                                    <div class="imgHover">
                                        <p class="text-center">Selecionar Imagen 1</p>
                                    </div>
                                </label>
                                <div class="form-admin__box inactivo">
                                    <input class="d-none" id="product-img" type="file" name="imagen" placeholder="Imagen del Producto" accept="image/*" >
                                </div>
                            </div>

                            
                            <!-- Columnas -->

                        </div>
            
            <button type="submit" class="boton-formulario">Crear Producto</button>

            
        </form>

        
    </main>
    <script src="/javascripts/imagenes.js"></script>
    <%- include('../partials/footer') %> 
</body>

    
</body>
</html>